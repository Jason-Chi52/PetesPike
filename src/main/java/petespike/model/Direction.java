package petespike.model;


/**
 * The {@code Direction} enum defines the four cardinal directions in which movements can occur
 * in a grid-based environment, such as in the Pete's Pike game.
 * This enum is used to specify the direction of movement of an entity within the game.
 */
public enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT;
}
